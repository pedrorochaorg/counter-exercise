<div class="container">
    <div class="layout">
        <%= form_for @hit, url: hit_path  do |f| %>
        <h2>Hit count!</h2>

        <h1><%= @hits.size %></h1>

        <br />

        <%= f.submit "Hit Me!" %>

        <% end %>
    </div>
</div>
<div class="background"></div>